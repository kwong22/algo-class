#ifndef GUARD_sort
#define GUARD_sort

// sort.h header file
long quick_sort(int*, int, int);
int choose_pivot_index(int*, int, int);
int partition(int*, int, int, int);
void swap(int*, int, int);

#endif
